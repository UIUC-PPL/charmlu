module luUtils {
  include "set";

  // Register the reducer function that handles reductions of MaxElm
  initnode void registerMaxElmReducer();

  class LUConfig;
  class CProxy_BlockScheduler;

  group LUMgr { };

  group PrioLU { entry PrioLU(int blkSize, int matSize); };
  group OnePerPE : CkArrayMap { entry OnePerPE(); };
  group PE2DTilingMap : CkArrayMap {
    entry PE2DTilingMap(int _peRows, int _peCols, int _peRotate, int _peStride, int _numBlks);
  };
};

